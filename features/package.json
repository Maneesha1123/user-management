{
  "name": "user-app-tests",
  "version": "1.0.0",
  "description": "Automated tests for user authentication app",
  "main": "index.js",
  "scripts": {
    "test:api": "mocha api-tests.js --timeout 10000",
    "test:ui": "mocha ui-tests.js --timeout 30000",
    "test": "npm run test:api && npm run test:ui"
  },
  "dependencies": {
    "axios": "^1.6.2",
    "chai": "^4.3.7",
    "chromedriver": "^138.0.1",
    "mocha": "^10.2.0",
    "selenium-webdriver": "^4.15.0"
  }
}